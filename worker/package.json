{
  "name": "outreach-pilot-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute DB --file ./migrations/0001_init.sql"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "stripe": "^13.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "@cloudflare/workers-types": "^4.20240618.0",
    "typescript": "^5.3.3"
  }
}
